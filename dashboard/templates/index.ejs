<%- include('blocks/header', {bot, user, path}) %>
  <div class="jumbotron">
    <div>
      <h1><%= bot.user.username %> Kontrol Paneli</h1>
      <h4><% if(bot.appInfo.description.length > 3) { %> <%= bot.appInfo.description %> <% } else { %>Herkeze Selam Ben Snax Botun Sahip Komutlar Ögrenmek İçin Altaki Komutlar Basın.<% }%></h4>
      <p><a class="btn btn-primary" href="/commands" role="button">Komutlar »</a></p>
    </div>
  </div>
<% include ./blocks/footer %>